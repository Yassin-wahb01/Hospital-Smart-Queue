# Vite client env vars
# No variables needed for local dev — Vite proxy handles /api → localhost:5000.
# No variables needed for production — Vercel proxy handles /api → Railway (see vercel.json).
# If you ever split client/server to different domains (not recommended), set:
# VITE_API_URL=https://your-server.railway.app/api/v1
